info:
  name: Create Client
  type: http
  seq: 15

http:
  method: POST
  url: http://localhost:5212/api/v1/clients
  body:
    type: json
    data: |
      {
        "name": "Audi"
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
