info:
  name: Create Category
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5212/api/v1/categories
  body:
    type: json
    data: |-
      {
        "title": "Health"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
