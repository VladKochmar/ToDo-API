info:
  name: Get Tasks
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:5212/api/v1/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
